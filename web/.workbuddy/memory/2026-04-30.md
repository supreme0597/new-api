# 2026-04-30

## 渠道分析 UI 重构

- **用量统计页** (`ChannelAnalytics/index.jsx`)：拆分"来源数据分析"卡片为4个独立卡片（来源对比/调用量占比/Token占比/使用趋势），来源对比独占一行，两个占比一行2列，使用趋势独占一行
- **来源详情页** (`ChannelAnalytics/Detail.jsx`)：拆分"模型数据分析"卡片为3个独立卡片（调用分布/Token分布/使用趋势），使用趋势卡片标题右侧改用 Tabs slash 切换 Token消耗/调用次数
- **来源详情页**：模型选择从 Checkbox 改为 Select multiple filterable 下拉框，移至页面头部时间范围左侧
- **来源详情页**：外层容器加 px-12 与列表页一致
- **路由清理**：删除 `/console/channel-analytics` 和 `/console/channel-analytics/:source` 路由，仅保留 `/channel-analytics` 和 `/channel-analytics/:source`，页面均无侧栏
- **i18n**：补充"使用趋势"翻译（zh-CN/en）

## Bug 修复

- Semi Card 的 `mb-4` 不生效：Semi Card 内联 `margin:0` 覆盖了 Tailwind，改用 `!mb-4`
- 饼图/折线图卡片高度过高：饼图 `h-96`→`h-80`，折线图 `h-96`→`h-72`

## 经验教训（已录入 .learnings）

1. **LRN-20260430-001**：Semi Card + Tailwind margin/padding/border-radius 必须用 `!` important 前缀
2. **LRN-20260430-002**：Tabs 拆分为独立卡片时需重新评估容器高度
3. **LRN-20260430-003**：路由修改需全局搜索旧路径，逐一修正所有引用点

