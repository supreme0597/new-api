# 2026-04-23

## 采样任务增加进度显示和详细日志

**问题：** 执行采集任务看不到进度，日志中也没有体现。

**实现：**
- `model/sampling_task.go`：
  - 新增 `SamplingTaskStatus` 结构体和全局状态变量，支持并发安全读写
  - `RunSamplingTask` 中增加防并发执行机制
  - 采样过程中实时更新进度（总任务数、已完成数、成功/失败数、当前渠道/模型、消息）
  - 所有关键节点输出 `[Sampling]` 前缀的详细日志（开始、渠道开始、每个模型的成功/失败、完成汇总）
- `controller/model_performance.go`：新增 `GetSamplingTaskStatus` API 返回当前采样状态
- `router/api-router.go`：添加 `/api/model-performance/sampling-status` 路由
- `web/src/pages/ModelPerformance/index.jsx`：
  - 点击"刷新采样"后立即查询状态并启动轮询（每 2 秒）
  - 采样进行中时显示进度卡片（进度条、完成数/总数、成功/失败数、当前正在采样的渠道和模型）
  - 采样完成后自动刷新排行榜数据
- i18n：添加「正在采样」「当前」翻译

## 修复：排行榜排序与排名标准不一致

**问题：** 排行榜中评分高的排在评分低的下面。例如：排名 8（score 16.1）排在排名 9（score 26.4）上面。

**根因：** 后端 `GetModelPerformanceList` 使用 `ORDER BY tps DESC, ttft ASC` 排序，但前端排名基于综合评分（score = TPS×60% + TTFT×40%）。排序标准和排名标准不一致。

**修复：** `model/model_performance.go` 中 `GetModelPerformanceList` 改为：
1. 查询所有符合条件的记录（不再用数据库分页）
2. 在内存中计算每条记录的 score
3. 按 score 降序排序
4. 手动分页并赋予正确排名

**潜在影响：** 数据量较大时会有内存开销，但测试渠道性能数据通常数量有限，影响可控。

## 渠道管理页面添加测试渠道筛选和列表标识

**需求：** 渠道管理页面上看不到哪些是测试渠道，需要加一个筛选，并在列表中显示是测试用的渠道。

**实现：**
- 前端 `ChannelsFilters.jsx`：添加「渠道类型」Select，选项为全部渠道/测试渠道/普通渠道，仅管理员可见
- 前端 `useChannelsData.jsx`：添加 `isTestChannelFilter` 状态，在 `loadChannels` 和 `searchChannels` 中传递 `is_test_channel` 参数
- 后端 `controller/channel.go`：在 `GetAllChannels` 和 `SearchChannels` 中读取 `is_test_channel` 查询参数并过滤
- 前端 `ChannelsColumnDefs.jsx`：在表格中添加「测试渠道」列（位于「类型」和「状态」之间），显示橙色「是」或灰色「否」Tag
- 前端 `useChannelsData.jsx`：`COLUMN_KEYS` 和 `getDefaultColumnVisibility` 添加 `TEST_CHANNEL`
- i18n：添加「渠道类型」「全部渠道」「普通渠道」「测试渠道」翻译

**行为：**
- 选择「测试渠道」只显示 `is_test_channel = 1` 的渠道
- 选择「普通渠道」只显示 `is_test_channel = 0` 或 NULL 的渠道
- 可与关键词搜索、分组筛选、模型筛选等条件组合使用
- 列表中每条渠道都显示是否为测试渠道

## 修复：排行榜采样次数始终显示0

**问题：** 模型响应速度排行榜中每条数据都显示"采样 0 次"。

**根因：** `ModelPerformanceItem` 结构体（返回给前端的 DTO）缺少 `sample_size` 字段。数据库查询时已通过 `model_performances.*` 取出 `sample_size`，但组装返回数据时未将其传入 `ModelPerformanceItem`，导致前端收到 `undefined`，`undefined || 0` 显示为 0。

**修复文件：** `model/model_performance.go`
1. 给 `ModelPerformanceItem` 添加 `SampleSize int \`json:"sample_size"\`` 字段
2. 在 `GetModelPerformanceList` 组装条目时传入 `r.SampleSize`

## 测试渠道与测试模型的概念澄清

用户提问：一个渠道的测试模型如何设置多个？

**现状：**
- `test_model`（默认测试模型）：仅用于管理员手动点击"测试"按钮时的默认模型，位于基础配置区
- `is_test_channel`（设为测试渠道）：位于高级设置 → 模型性能采样设置，启用后该渠道所有模型都会被定时采样

**关键代码：**
- `controller/channel-test.go:82-92`：`test_model` 只影响手动测试，没填就取模型列表第一个
- `model/sampling_task.go:42-46`：采样任务已遍历 `channel.GetModels()` 全部模型

**结论：** `test_model` 和 `is_test_channel` 是两个独立功能，前者服务手动测试，后者服务自动采样。测试渠道的自动采样已经覆盖该渠道所有模型，无需额外设置"多个测试模型"。
