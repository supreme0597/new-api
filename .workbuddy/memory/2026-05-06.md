# 2026-05-06

## 数据看板：管理员隐藏账户数据卡片

- 需求：管理员/超级管理员查看数据看板时，去掉「账户数据」卡片（当前余额、历史消耗），因为是内部个人使用，不存在账户数据概念
- 修改文件：
  - `web/src/hooks/dashboard/useDashboardStats.jsx` - 新增 `isAdminUser` 参数，管理员时用 spread 空数组跳过账户数据分组
  - `web/src/components/dashboard/index.jsx` - 传入 `dashboardData.isAdminUser` 给 `useDashboardStats`
- 逻辑：`isAdmin()` 返回 true（role >= 10）时，groupedStatsData 不包含「账户数据」分组，普通用户仍可看到
- 补充：资源消耗卡片中的「统计额度」也对管理员隐藏，只保留「统计Tokens」

## 用量统计：筛选器交互改为直觉逻辑

- 需求：将「来源视角选模型、模型视角选来源」的反直觉设计改为直觉逻辑，并参考 coding.15o.cc/trends 的折叠选择方式
- 修改文件：
  - `web/src/pages/ChannelAnalytics/index.jsx`
    - 状态拆分：来源视角（svSources/svModels/svSourcesExpanded/svModelsExpanded）和模型视角（mvModels/mvSources/mvModelsExpanded/mvSourcesExpanded）各自独立
    - FilterPills 组件新增「全部」按钮、折叠/展开、展示全部按钮带剩余数量标记
    - 来源视角：始终展示来源汇总线，选中模型后额外展示「来源/模型」细分线
    - 模型视角：每条线 = 「来源/模型名」
    - 数据加载后自动全选当前视角的主维度（来源视角全选来源，模型视角全选模型）
    - 切换视角时自动为目标视角补全默认选中
    - API 传参按视角分别映射到 sources/model_names
  - `web/src/i18n/locales/zh-CN.json` / `en.json` - 添加「展示全部」翻译
- Go 后端无需修改，现有 `/model-comparison/trend` 接口已支持 sources + model_names 双参数过滤

### 级联筛选（后续追加）

- 需求：来源和模型之间添加级联关系，选择来源后模型列表自动过滤为选中来源下的模型
- 实现：
  - 来源视角：`svAvailableModels` 根据 `svSources` 从 `comparisonItems` 中过滤
  - 模型视角：`mvAvailableModels` 根据 `mvSources` 过滤
  - 自动过滤：来源变化后，已选模型中不在可用列表的自动移除
  - FilterPills 模型列表改用可用模型而非全部模型

### 去掉视角切换（后续追加）

- 需求：用户觉得来源视角和模型视角区别不大，要求去掉
- 修改：
  - 去掉 `<Tabs>` 视角切换，只保留 Token 消耗 / 调用次数指标切换
  - 合并两套筛选状态为统一的 `selectedSources` + `selectedModels`
  - `SourceTrendChart` 固定每条线为「来源 / 模型名」，去掉 `viewMode` 参数
  - 筛选区固定显示「来源」+「模型」两个 Select 多选，保持级联关系
  - 清理所有 `viewMode`/`handleViewModeChange`/`svSources`/`svModels`/`mvSources`/`mvModels` 相关代码
