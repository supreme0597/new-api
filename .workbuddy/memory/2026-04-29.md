# 2026-04-29 工作记录

## 用量统计增加自定义时间范围选择器 ✅

**问题**：用量统计只能选择固定条件（近1天/近7天/近30天），数据看板可以自由选择时间段，两个页面时间条件不一致，无法精确对比验证数据。

**修改**：
1. `web/src/pages/ChannelAnalytics/index.jsx`（首页）：
   - 导入 `DatePicker`
   - 增加 `customRange` 状态（`[Date, Date]`）
   - `getTimestamps` 支持 `range === 'custom'` 时返回自定义时间戳
   - `range` Select 增加「自定义」选项
   - 选中自定义时显示 `DatePicker type="dateTimeRange"`
   - 统计时间文本根据自定义/快捷选项分别显示

2. `web/src/pages/ChannelAnalytics/Detail.jsx`（详情页）：
   - 同样的改造逻辑
   - `rangeLabel` 支持自定义时间范围显示

## 用量统计详情页柱状图改面积折线图 ✅

**问题**：详情页用量趋势和 Token 消耗的柱状图视觉效果不佳。

**修改**：`web/src/pages/ChannelAnalytics/Detail.jsx` - `UsageTrendChart` 组件：
1. 图表类型从 bar（柱状图）改为 area + line（面积折线图）
2. 面积填充使用渐变（实色→透明），增加视觉层次
3. 调用次数用蓝色 `#1664FF`，Token 消耗用绿色 `#3CC780`
4. 去掉柱子上方的标签（避免密集），改为 hover 数据点 + 虚线十字准星
5. 使用 common 图表类型实现面积+折线组合

## 修复用量统计（渠道分析）跨库查询失败 ✅

**问题**：测试环境部署后用量统计看不到数据，30天条件不生效。

**根因**：`LOG_DB` 和 `DB` 可能是不同的数据库（通过 `LOG_SQL_DSN` 环境变量配置）。原代码在 `LOG_DB` 上执行 `JOIN channels ON logs.channel_id = channels.id`，但 `channels` 表只在主库 `DB` 中，日志库 `LOG_DB` 没有 `channels` 表，导致 JOIN 失败，查询返回空数据。

**修复方案**：移除所有 `JOIN channels`，改为两步查询：
1. 先从主库 `DB` 查询渠道ID和来源映射
2. 再用 `IN` 条件在日志库 `LOG_DB` 上过滤

**提交**：`a6138634`

## 修复用量统计导航入口在测试环境不显示 ✅

**问题**：测试环境看不到用量统计入口。

**根因**：`useNavigation.js` 过滤逻辑 `modules[link.itemKey] === true`，后端 `HeaderNavModules` 配置不包含 `channelAnalytics` 字段时，`undefined !== true` 导致入口被隐藏。

**修复**：`undefined` 视为 `true`（默认显示），只有显式设为 `false` 才隐藏。

**提交**：`74601d05`

## 渠道分析重命名为用量统计 ✅

**提交**：`3c26f2b4` - 导航栏、页面标题、面包屑、翻译统一改为「用量统计」

## 渠道分析入口移到顶部导航栏 ✅

**提交**：`e79a53b8` - 与首页/控制台/模型广场并行，仅管理员可见

## 图表时间格式和Token单位优化 ✅

- X轴设为 `type: 'band'` 阻止 VChart 自动时间解析，去掉 Z 后缀
- 根据粒度调整时间显示：hour→MM-DD HH:mm，day/week→MM-DD
- Token 消耗柱状图 label/tooltip/Y轴使用 formatLargeNumber（K/M 格式）

## 修复 MySQL DATE() 返回 ISO 格式时间 ✅

**问题**：测试环境（MySQL）用量趋势的时间格式显示为 `04-25T00:00:00Z`。

**根因**：项目自动为 MySQL DSN 加 `parseTime=true`，导致 go-sql-driver 将 `DATE()` 返回的 `DATE` 类型自动转为 Go 的 `time.Time`。GORM 扫描到 struct 的 `string` 字段后，JSON 序列化走 `time.Time.MarshalJSON()`，输出 ISO 格式 `2026-04-25T00:00:00Z`。

**修复**：`getTimeBucketExpr` 中 day 粒度从 `DATE(FROM_UNIXTIME(...))` 改为 `DATE_FORMAT(FROM_UNIXTIME(...), '%Y-%m-%d')`。`DATE_FORMAT` 返回 `VARCHAR` 字符串，不会被驱动转为 `time.Time`。

**提交**：`74358dbc`

## 修复前端 formatTime 兼容 ISO 格式 ✅

**问题**：前端 `formatTime` 用 `split(' ')` 按空格分割，ISO 格式没有空格导致显示异常。

**修复**：先替换 `T` 为空格、去掉 `Z`，再按原逻辑处理。兼容 MySQL 和 ISO 两种格式。

**提交**：`8ea38708`

## 修复用量统计跳转 Dashboard 看不到数据 ✅

**问题**：从用量统计详情页点击用户跳转到 Dashboard 看不到数据。

**根因（3个问题）**：
1. 跳转 URL 只传了 `username`，没传时间范围 → Dashboard 用默认时间（1天），不是用量统计当前的30天
2. Dashboard 没从 URL 读取时间戳参数 → 无法同步查询条件
3. 后端 `GetQuotaDataByUsername` 没有 `GROUP BY model_name, created_at` → 返回的数据格式与 `GetAllQuotaDates` 不一致

**修复**：
1. `Detail.jsx` 跳转时追加 `start_timestamp` 和 `end_timestamp` 到 URL
2. `useDashboardData.js` 从 URL 参数读取时间戳，覆盖默认时间范围
3. `model/usedata.go` 中 `GetQuotaDataByUsername` 添加 `SELECT ... GROUP BY model_name, created_at`，与 `GetAllQuotaDates` 保持一致

## 修复 Dashboard 图表排序和柱子宽度 ✅

**问题**：
1. Dashboard 用量趋势图表时间排序错乱
2. 模型调用次数排行柱状图柱子太粗

**根因**：
1. VChart 自动将 X 轴 `Time` 识别为时间轴，尝试解析 `MM-DD HH:00` 格式字符串导致排序错误
2. 没有设置 `bandPadding`，VChart 默认柱子占满分类空间

**修复**：
1. `spec_line`（模型消耗分布）和 `spec_model_line`（调用趋势）添加 `axes: [{ orient: 'bottom', type: 'band' }]`，阻止自动时间解析
2. `spec_rank_bar`（模型调用次数排行）添加 `axes` 并设置 `bandPadding: 0.3, paddingInner: 0.4`，减小柱子宽度

## 用量统计饼图添加占比百分比 ✅

**问题**：用量统计页面「各来源调用量占比」和「各来源 Token 消耗占比」饼图没有显示比例。

**修改**：`web/src/pages/ChannelAnalytics/index.jsx` - `SourcePieChart` 组件：
1. label 从只显示名称改为显示来源名称 + 百分比（如 `OpenAI\n45.2%`）
2. tooltip 同时显示具体数值和百分比（如 `1,234,567 (45.2%)`）
3. 左侧图例也显示占比（如 `OpenAI  45.2%`）
4. 百分比基于当前饼图数据总值实时计算

## 修复用量统计趋势图表时间排序错乱 ✅

**问题**：用量统计首页「各来源使用次数趋势」和详情页「用量趋势」选择30天按天粒度时，时间排序错乱。

**根因**：chartData 构建时没有按 Time 排序，VChart band 轴按数据顺序渲染。

**修复**：
1. `ChannelAnalytics/index.jsx` - `SourceTrendChart` 的 chartData 添加 `result.sort((a, b) => a.Time.localeCompare(b.Time))`
2. `ChannelAnalytics/Detail.jsx` - `UsageTrendChart` 的 chartData 添加 `.sort((a, b) => a.Time.localeCompare(b.Time))`

## 模型对比功能方案设计 ✅

**需求**：用量统计增加模型维度对比，包括模型使用次数占比、Token消耗占比、使用趋势。

**推荐方案 C：Tab 切换 + 详情下钻**
- 首页增加 Tab 栏（来源视图 / 模型对比），切换后展示模型维度数据
- 点击模型名可下钻到单模型详情页

**图表设计**：
1. 模型调用次数占比 - 环形饼图（Donut），Top 8 + 其他
2. 模型 Token 消耗占比 - 环形饼图，同上
3. 模型使用趋势 - 多系列折线图，支持切换调用次数/Token消耗
4. 模型排行表 - 表格 + 迷你条形图显示占比

**草图文件**：`brain/8da5c79c762d49b1bd7cfd106623cfee/model-comparison-mockup.html`

## 用量统计详情页增加模型维度分析 ✅

**需求变更**：在现有用量统计详情页中增加模型维度分析，而非新建页面。

**实现内容**：

### 后端改动
1. **新增数据结构**（`model/channel_analytics.go`）：
   - `ChannelModelStat` - 按模型分组的统计（model_name, call_count, token_count, active_users）
   - `ChannelModelTrendPoint` - 模型趋势数据点（time, model_name, call_count, token_count）

2. **新增接口**（`controller/channel_analytics.go` + `router/api-router.go`）：
   - `GET /api/channel-analytics/source/:source/models` - 获取来源下各模型统计
   - `GET /api/channel-analytics/source/:source/model-trend` - 获取来源下各模型趋势

3. **用户排行增加模型过滤**（`model/channel_analytics.go`）：
   - `GetChannelSourceUserRanking()` 增加 `modelName` 参数
   - 当 `modelName` 不为空时，添加 `logs.model_name = ?` 过滤条件

### 前端改动（`web/src/pages/ChannelAnalytics/Detail.jsx`）
1. **新增组件**：
   - `ModelPieChart` - 模型占比环形饼图（复用首页 SourcePieChart 风格）
   - `ModelTrendChart` - 多模型折线图（参考数据看板调用趋势，`selectMode: 'single'`）

2. **页面布局改造**：
   - 在头部下方增加**双饼图**（各模型调用次数占比 + 各模型 Token 消耗占比）
   - 用量趋势改为**多模型折线图**（默认显示全部模型）
   - 图表下方增加**模型选择器**（Checkbox 列表，支持全选/清空）
   - 活跃用户排行根据选中模型动态过滤（单选时显示该模型用户排行）

3. **数据加载**：
   - `loadData()` 同时请求详情、模型统计、模型趋势、用户排行
   - `loadUsers()` 当模型选择变化时独立刷新用户排行
   - 默认全选所有模型

### 翻译更新
- `zh-CN.json` / `en.json` 新增：各模型调用次数占比、各模型 Token 消耗占比、模型使用趋势、默认显示全部模型、点击图例可聚焦单个模型、选择模型、全选、清空
